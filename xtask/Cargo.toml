[package]
name = "xtask"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false

# Repo automation: the golden-output parity harness (`xtask parity`) that
# diffs the Rust binary's `--json` against per-fixture goldens. Not shipped.

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
toml = "1.1"
