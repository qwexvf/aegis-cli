name: ci

on:
  push:
    branches: [main]
  pull_request:

env:
  CARGO_TERM_COLOR: always

jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262  # v4
      - uses: dtolnay/rust-toolchain@4cda84d5c5c54efe2404f9d843567869ab1699d4  # stable
        with:
          components: rustfmt, clippy
      - uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32  # v2
      - name: format
        run: cargo fmt --all --check
      - name: clippy
        run: cargo clippy --workspace --all-targets -- -D warnings
      - name: test
        run: cargo test --workspace

  lean-build:
    # Guards the lean-build promise: each feature-gated crate must compile with
    # default features OFF and only a minimal feature set on — so a leaked
    # optional dep or a missing `#[cfg(feature = ...)]` is caught here, not by a
    # downstream consumer trying to build lean. Covers the zero-feature floor
    # plus each feature that pulls an optional dep (regex / quick-xml / grammars
    # / tree-sitter) in isolation.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262  # v4
      - uses: dtolnay/rust-toolchain@4cda84d5c5c54efe2404f9d843567869ab1699d4  # stable
      - uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32  # v2
      - name: lean feature builds
        run: |
          set -euxo pipefail
          # lockfile: zero ecosystems, a pure one, and the optional-dep ones.
          cargo build -p aegis-lockfile --no-default-features
          cargo build -p aegis-lockfile --no-default-features --features npm
          cargo build -p aegis-lockfile --no-default-features --features maven
          cargo build -p aegis-lockfile --no-default-features --features hex
          cargo build -p aegis-lockfile --no-default-features --features conda
          cargo build -p aegis-lockfile --no-default-features --features conan
          cargo build -p aegis-lockfile --no-default-features --features julia
          # ast grammars: zero, one native, the ruby-reuse (cocoapods), and the
          # new grammars in isolation.
          cargo build -p aegis-ast --no-default-features
          cargo build -p aegis-ast --no-default-features --features js
          cargo build -p aegis-ast --no-default-features --features cocoapods
          cargo build -p aegis-ast --no-default-features --features kotlin
          cargo build -p aegis-ast --no-default-features --features elixir
          cargo build -p aegis-ast --no-default-features --features bash
          # heuristics: zero, and each optional-dep detector in isolation.
          cargo build -p aegis-heuristics --no-default-features
          cargo build -p aegis-heuristics --no-default-features --features secrets
          cargo build -p aegis-heuristics --no-default-features --features install-hook
          cargo build -p aegis-heuristics --no-default-features --features maintainer
          # sbom emitters + registry fetchers.
          cargo build -p aegis-sbom --no-default-features
          cargo build -p aegis-sbom --no-default-features --features cyclonedx
          # pkgbuild has no optional deps (regex is core to every rule);
          # build it anyway so a future feature gate cannot regress unnoticed.
          cargo build -p aegis-pkgbuild --no-default-features
          cargo build -p aegis-registry --no-default-features
          cargo build -p aegis-registry --no-default-features --features npm
          # net: zero-transport floor + the cassette record/replay feature.
          cargo build -p aegis-net --no-default-features
          cargo build -p aegis-net --no-default-features --features cassette
          cargo build -p aegis-net --no-default-features --features "ureq-backend cassette"

  audit:
    # RustSec advisory gate over the real dependency graph (Cargo.lock only —
    # the deliberately-vulnerable fixtures under examples/ are separate manifests
    # and are NOT in the workspace lockfile, so they never trip this). Fails the
    # build when a crate we actually depend on gains an advisory.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262  # v4
      - uses: dtolnay/rust-toolchain@4cda84d5c5c54efe2404f9d843567869ab1699d4  # stable
      - uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32  # v2
      - name: install cargo-audit
        run: cargo install cargo-audit --locked
      - name: cargo audit
        run: cargo audit

  parity:
    # Command-parity gates. Diff the Rust binary against goldens captured from
    # the Go reference (aegis v0.29). Goldens are committed (examples/), so no
    # Go toolchain is needed here — refresh them with
    # `cargo run -p xtask -- <gate> --record` when the Go binary is local.
    # ci-parity is offline too: it replays a committed HTTP cassette
    # (examples/ci-parity-cassettes/), re-captured with
    # `cargo run -p xtask -- ci-parity --record-cassettes` when a pinned
    # advisory set changes.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262  # v4
      - uses: dtolnay/rust-toolchain@4cda84d5c5c54efe2404f9d843567869ab1699d4  # stable
      - uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32  # v2
      - name: analyze-parity (Rust vs Go-captured goldens)
        run: cargo run -q -p xtask -- analyze-parity
      - name: sbom-parity (Rust vs Go-captured goldens)
        run: cargo run -q -p xtask -- sbom-parity
      - name: ci-parity (Rust vs Go golden, replayed HTTP cassette)
        run: cargo run -q -p xtask -- ci-parity
