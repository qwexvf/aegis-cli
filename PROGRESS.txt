===============================================================================
 aegis-rs — Go → Rust migration progress
===============================================================================

Goal: clean-room rewrite of aegis-cli (Go, ~59k LOC, v0.29) into Rust, to full
feature parity, then flip the default. Go stays frozen at v0.29 until parity.

Location : /home/qwexvf/projects/aegis/aegis-rs  (own standalone git repo)
Branch   : port/domain-lockfile
Plan     : ~/.claude/plans/twinkly-scribbling-gadget.md

Status @ this snapshot:
  12 workspace members (11 crates + xtask parity harness) · clippy + rustfmt
  clean.
  13 CLI commands: parse / analyze / ci / run / sbom / fix / allowlist / image /
  explain / reach / hook / actions / snapshot.

  *** PARITY COMPLETE FOR ALL 3 CORE COMMANDS — 3 COMMITTED GATES ***
  The Go aegis-cli source is local (../aegis-cli) + Go 1.26 installed, so real
  Go-vs-Rust diffs run. Only P11 (release cutover) remains.
  - analyze: `xtask analyze-parity` — 36/36 incident fixtures across ALL 13
    ecosystems match Go (verdict+score+capabilities). Committed goldens, no Go
    at check time. IN CI (.github/workflows/ci.yml).
  - ci: `xtask ci-parity` — 6 fixtures / 5 ecosystems (npm/pypi/crates/go/gem)
    match Go. Enrich model ported (per-dep tarball fetch+scan → verdict +
    advisories, Go-shaped report), reachability downgrade ported (npm-gated),
    advisory fixed_in+in_kev added. Network-dependent → OPT-IN, not in CI.
  - sbom: `xtask sbom-parity` — CDX+SPDX vs Go goldens, 4/4. Offline +
    deterministic → IN CI. Fixed CDX $schema + empty-dependsOn shape.
  - explain / snapshot: CLI-model-divergent (see NEXT #4); per-package caps ≡
    analyze (gated), unique logic (model-doc / drift) golden-/integration-tested.
  Parity bugs found + fixed by diffing against Go: install-hook base flag;
  R + Perl obfuscated-payload regexes; .ex/.exs analyzable-source (Elixir);
  Java tree-sitter grammar (maven); advisory alias-over-dedup (ci 10 vs 8);
  install-hook fingerprint population; ci advisory fixed_in/in_kev; ci
  reachability-unused downgrade; CDX $schema + empty dependsOn.

  Feature surface (all CLI-wired + LIVE-verified):
  - reachability (aegis-reach): imports + used-symbols (js/ts/py/go/php; ruby
    excluded by design) + intra-file call graph + cross-file transitive callers
    (`reach --function [--transitive]`).
  - allowlist: builtin + config/`--allowlist` rules APPLIED in analyze/run/ci
    (apply_allowlist suppresses flags + subtracts weight).
  - sbom: PURL + hashes + dep graph + license fields (`sbom --online` →
    CycloneDX licenses[] / SPDX concluded-declared) + advisory/vuln fields
    (CycloneDX vulnerabilities[] / SPDX SECURITY refs).
  - image: local tarball OR registry pull; generic /v2 Bearer-challenge auth
    (`--username/--password` or AEGIS_REGISTRY_USER/PASS) for private images.
  - ci: per-dep enrich (registry::fetch_npm_source → scan → verdict) + OSV +
    GHSA (rate-limit-aware) + EPSS + KEV; Go-shaped report, verdict fail-on.
  - fix: shared crate::semver engine (prerelease-correct ordering).
  - 13 AST grammars (js/py/ruby/rust/go/php/csharp/java/haskell/lua/gleam/
    dart/cocoapods, native — no CGO).

  SECURITY E2E SUITE: crates/aegis-cli/tests/incidents.rs — reconstructs real
  supply-chain attacks + true-negatives + fleet scan; drives the real binary.
  Concurrency = rayon (all cores), NOT tokio.
  aegis-cli main.rs modularized (commands/scan/enrich/util).

-------------------------------------------------------------------------------
 PHASE STATUS  (plan has 12 phases, 0-11)
-------------------------------------------------------------------------------
  P0  scaffold + parity harness ...... PARITY LIVE (Go binary available!)  the Go
                                       aegis-cli source is local (../aegis-cli) +
                                       Go 1.26 installed → real Go-vs-Rust diffs.
                                       Bridge: Go `analyze <spec> --local <dir>` ==
                                       Rust `analyze <dir>`; both AST-scan a local
                                       tree. Swept examples/incidents/npm (6 fixtures)
                                       → after the install-hook fix (below) Rust
                                       matches Go 6/6 on verdict+score+capabilities
                                       (coa 140, event-stream 265, node-ipc 75, rc
                                       140, tanstack 340, ua-parser 135). FIRST REAL
                                       PARITY BUG FOUND + FIXED. FULL-CORPUS SWEEP
                                       (analyze, all 13 ecosystems, 36 incident
                                       fixtures): 36/36 match Go on verdict+score+
                                       caps. Fixed along the way: install-hook base
                                       flag; R + Perl obfuscated-payload regexes;
                                       .ex/.exs added to analyzable-source (hex/
                                       Elixir); Java tree-sitter grammar added
                                       (tree-sitter-java + queries/java.scm ported
                                       from Go) → maven log4j+spring now match →
                                       36/36, ALL 13 ecosystems green. Harness
                                       (xtask parity)
                                       [--record]` runs the Rust binary per case
                                       (xtask/parity_cases.toml), structurally diffs
                                       --json against a golden (order-independent
                                       jsondiff engine, 10 tests). Unblocked the
                                       Go/Rust arg divergence via the GOLDEN model:
                                       per-case args drive Rust, golden is captured
                                       once from the reference — the args→golden
                                       pairing IS the schema-mapping layer. 4 cases
                                       LIVE + green (parse npm + cargo, allowlist,
                                       explain — all offline/deterministic); doubles
                                       as a Rust-CLI regression suite now.
                                       AUTOMATED GATE: `xtask analyze-parity`
                                       vendors the incident corpus + Go-captured
                                       goldens (examples/incidents/<eco>/<fix>/
                                       analyze.golden.json) → Rust-vs-golden at
                                       check time (no Go needed). 36/36 green.
                                       Wired into .github/workflows/ci.yml
                                       (fmt/clippy/test + parity). Refresh goldens
                                       with `--record` when the Go binary is local.
                                       COMMAND PARITY STATUS: analyze 36/36 ✓.
                                       sbom = content-parity ✓ (CDX components +
                                       SPDX packages match Go; only the root
                                       component name differs by --project default).
                                       ci = ENRICH MODEL PORTED: Rust ci now matches
                                       Go — fetches each dep's tarball
                                       (registry::fetch_npm_source, npm), AST +
                                       heuristics scans it → per-dep capability
                                       verdict, folds in advisory severity (verdict =
                                       max(cap, VerdictForAdvisories)), emits the
                                       Go-shaped report {project, fail_on, enriched,
                                       passed, summary{safe/review/prompt/blocked},
                                       findings[{verdict, risk_score, flags,
                                       advisories}]}. --fail-on is now a VERDICT
                                       threshold (safe|review|prompt|block, default
                                       block), not severity; hook/actions updated.
                                       CI PARITY COMPLETE — LIVE vs Go on lodash
                                       4.17.4: verdict=block, score=55,
                                       flags=[unlisted-large-file], summary, AND all
                                       10 advisories match. (Fixed the last diff:
                                       advisories_by_key was alias-deduping OSV's
                                       mutual alias-pairs 10→8; now id-only dedup,
                                       also corrects fix/sarif.) npm enrich only;
                                       other-ecosystem source fetchers = follow-up.
  P1  domain ......................... DONE
  P2  lockfile (24 parsers) .......... DONE
  P3  net / vuln / registry .......... DONE (core)  tail: GHSA, npm attestations,
                                       per-ecosystem version resolvers
  P4  heuristics ..................... DONE  all detector fns ported: secrets,
                                       typosquat, source-patterns, binary-dropper,
                                       vcs-dep, optional-git-dep, install-hook,
                                       unlisted-payload, go-retract, maintainer x3,
                                       tarball-drift (pure drift.Diff engine +
                                       monorepo-mismatch confidence cutoff) + npm
                                       manifest parser + go.mod retract parser.
                                       tarball-drift is now LIVE: the GitHub
                                       git-tree fetch (aegis-registry::github) is
                                       wired into `analyze --online` (verified LIVE:
                                       local tree w/ extra file vs lodash@4.17.21
                                       upstream tag → tarball-source-drift).
  P5  AST + reach .................... DONE (slice)  ALL 13 grammars (js/py/ruby/
                                       rust/go/php/csharp/java/haskell/lua/gleam/
                                       dart/cocoapods, native — no CGO). FULL JS taint
                                       (symtab + numeric-array + spread fold +
                                       tainted-sink). aegis-reach crate: import
                                       extraction + reachability_of for all 5 langs
                                       (js/ts/py/go/php/ruby); `reach` CLI command
                                       LIVE. USED-SYMBOLS (function-level): js/ts +
                                       python + go (selector + qualified-type) + php
                                       (use-binding → :: member) done; Ruby excluded
                                       by design (require/gem bind no local — needs
                                       project-wide constant resolution). CALL-GRAPH
                                       (additive, no dead-code pruning → no FN risk):
                                       js/ts + python + go + php intra-file caller→
                                       callee edges (CallNode/call_graph*) + used-
                                       symbol→enclosing-function attribution
                                       (SymbolSite/used_symbol_sites*). WIRED LIVE:
                                       functions_reaching (routes all 4 langs) +
                                       `reach --function` now reports which project
                                       functions reach the vulnerable symbol (↳
                                       file:line fn / json callers[]). CROSS-FILE
                                       DONE: functions_reaching_transitive walks
                                       call-graph callers across files (name-based,
                                       over-approximate → additive/no-FN); wired as
                                       `reach --function --transitive` (direct vs
                                       transitive tagged, LIVE-verified cross-file).
                                       Call-graph follow-up COMPLETE — P5 has no
                                       open reachability items.
  P6  image (OCI) .................... DONE (slice)  aegis-image crate: docker-save +
                                       OCI-layout tarball extract (layer overlay,
                                       whiteout/opaque-dir handling) + shallow risk
                                       scan (dropper/hidden-ELF/embedded-manifest).
                                       tar+flate2 pure-Rust, no registry pull.
                                       `image` CLI command: scans a local tarball
                                       OR pulls by ref (`--ref alpine:latest`,
                                       ghcr.io/...) via OCI distribution
                                       (token → manifest → blobs → overlay), LIVE-
                                       verified against Docker Hub. THREE scan tiers:
                                       shallow + source-pattern + tree-sitter AST.
                                       AUTH DONE: generic /v2/ ping → Www-Authenticate
                                       Bearer challenge → token flow (one path for
                                       docker.io/ghcr/GCR/private); `--username/
                                       --password` or AEGIS_REGISTRY_USER/PASS →
                                       Basic auth on token req (private images).
                                       aegis-net HttpResponse now carries headers.
                                       LIVE re-verified: alpine:latest via new flow.
  P7  sbom (CycloneDX/SPDX/SARIF) .... DONE  aegis-sbom crate: PURL builder
                                       (canonical, matches Go), CycloneDX 1.5 +
                                       SPDX 2.3 + SARIF 2.1.0 JSON emitters (pure,
                                       deterministic, hand-rolled base64 → no deps
                                       beyond serde). `sbom` command (--format
                                       cyclonedx|spdx) + `ci --sarif`. All
                                       LIVE-verified + integration-tested. LICENSE
                                       FIELDS DONE: Dependency.license added;
                                       `sbom --online` resolves SPDX license via
                                       LicenseFetcher (rayon over deps) → CycloneDX
                                       licenses[] + SPDX licenseConcluded/Declared
                                       (LIVE: lodash → MIT; offline stays
                                       NOASSERTION/omitted). ADVISORY FIELDS DONE:
                                       `sbom --online` resolves advisories (OSV+GHSA,
                                       EPSS/KEV) via advisories_by_key → CycloneDX
                                       vulnerabilities[] (id/source/rating/affects)
                                       + SPDX SECURITY externalRefs (LIVE: lodash
                                       4.17.4 → 8 vulns both formats). P7 tail clear.
  P8  usecase ....................... DONE (functional)  analyze wires npm/go
                                       manifest parsers → NormalizedPackage +
                                       `--online` npm-packument maintainer x3 +
                                       provenance flag (LIVE). Enrich concurrency IS
                                       present (rayon over deps in cve_findings/run).
                                       `snapshot` command adds behavioral-drift
                                       detection (capability diff between versions —
                                       the takeover signal). Go's exact
                                       snapshot.Enrich actor-model not ported (the
                                       rayon design supersedes it for a CLI).
  P9  presenter ..................... DONE (non-TUI)  text/json + SARIF 2.1.0 +
                                       CycloneDX/SPDX + `explain` risk-model doc.
                                       FOLLOW-UP: interactive live-region TUI —
                                       deliberately deferred (JSON+SARIF already
                                       feed dashboards/Code-Scanning; a TUI adds
                                       little for a CI-first scanner).
  P10 cli ........................... DONE  13 commands: parse/analyze/ci/run/sbom/
                                       fix/allowlist/image/explain/reach/hook/
                                       actions/snapshot. hook = git pre-commit
                                       installer; actions = GitHub Actions workflow;
                                       snapshot = drift detection; explain = risk
                                       model. main.rs modularized (148 lines).
  P11 parity gate + cutover ......... GATE DONE, CUTOVER PENDING. Parity gate
                                       shipped: 3 committed harnesses (analyze-
                                       parity + sbom-parity in CI, ci-parity
                                       opt-in) prove Go==Rust for all 3 core
                                       commands. Remaining = release infra only
                                       (cosign keyless + SLSA provenance via
                                       sigstore-rs, CI secrets), then flip the
                                       default. Deferred by owner — do after.

  Rough count: P1/P2/P3/P4/P5/P6/P7/P8/P9/P10 done or done-to-a-strong-slice
  (with documented deep follow-ups); P0/P11 documented-blocked (architectural,
  not a code gap). Foundational + detection + output + CLI surface complete;
  remaining = full reachability (call-graph), OCI registry pull, the parity gate.

-------------------------------------------------------------------------------
 DESIGN DECISIONS
-------------------------------------------------------------------------------
- Compile-time feature flags everywhere (user request): each lockfile ecosystem,
  each tree-sitter grammar, each heavy subsystem behind a Cargo feature, all on
  by default. Lean builds strip what they don't need, e.g.:
      cargo build --no-default-features --features npm,pypi,rust
  Heavy deps (regex, quick-xml, tree-sitter grammars) are optional, pulled in
  only by the features that need them. Verified: npm-only builds exclude them.

- Transport = ureq (blocking, pure-Rust, NO tokio) instead of the plan's reqwest.
  Reason: CLI is blocking + thread-pool concurrent; dropping tokio keeps the
  binary small (serves the lean-build goal). Behind an HttpClient trait, swappable.

- Native tree-sitter grammar crates → NO CGO / no C toolchain (the headline win).
  .scm queries port verbatim from the Go tree.

- domain crate stays dependency-free (mirrors Go's stdlib-free internal/domain);
  the CLI owns its serde wire DTOs.

-------------------------------------------------------------------------------
 CRATES  (✅ done · 🟡 partial)
-------------------------------------------------------------------------------
✅ aegis-domain     risk scoring, capabilities (23), verdict thresholds,
                    advisory/severity types. Zero deps. 14 tests.
✅ aegis-lockfile   all 24 lockfile parsers, every ecosystem:
                    npm(package-lock/yarn/pnpm/bun), pypi(poetry/uv/pipfile/req),
                    cargo, go, composer, nuget, cran, swift, gemfile,
                    maven(pom+gradle), hex(gleam+mix), pubspec, cocoapods,
                    cpan, hackage(cabal+stack). 29 tests.
✅ aegis-net        HttpClient trait + MockHttpClient + UreqClient backend +
                    crash-safe DiskCache (atomic write + TTL). 4 tests.
✅ aegis-vuln       OSV.dev (full CVSS v3 base-score formula → verified 9.8),
                    EPSS (FIRST.org), KEV (CISA). 12 tests.
✅ aegis-registry   license fetchers (npm/pypi/crates/rubygems/nuget) + deps.dev
                    package-health (deprecation) + advisory lookup + npm
                    packument maintainer-signal fetch (publish times, _npmUser,
                    weekly downloads, yank detection) + GitHub git-tree adapter
                    (repo-field parse, tag candidates, recursive tree fetch →
                    repo_files for tarball-drift). 22 tests.
🟡 aegis-heuristics secrets, typosquat (Levenshtein + embedded top-lists),
                    source-patterns (obfuscated-payload / suspicious-url / IDN /
                    known-malware-ioc / curl|sh shell-fetcher, with concat-collapse
                    de-obfuscation), binary-dropper (native exe/script outside
                    ecosystem convention), vcs-dep + optional-git-dep (Mini
                    Shai-Hulud vector), install-hook (full malware-pattern matcher:
                    curl|sh, inline-eval w/ benign carve-out, base64-piped,
                    C2-host, silent-exit, non-standard-runtime). npm manifest
                    parser populates Deps + Hooks. Feature-gated (binary-dropper /
                    vcs-dep / install-hook / unlisted-payload / go-retract /
                    maintainer / tarball-drift); lean builds exclude
                    regex+serde_json+semver+time. Also: unlisted-large-file, go.mod
                    retract (version+range, semver-compared), maintainer
                    hijack/yank/handover (registry signal, injected clock),
                    tarball-drift (pure diff engine: whitelist + minimal glob +
                    hook-path extraction + monorepo-subdir cutoff). 86 tests.
🟡 aegis-ast        tree-sitter AST scanner — 12 grammars (js/py/ruby/rust/go/
                    php/csharp/haskell/lua/gleam/dart + cocoapods=.podspec via
                    ruby grammar), all native (no CGO), .scm queries verbatim
                    from the Go tree. capability + env-read + evidence + JS
                    constant-fold taint. 32 tests (each grammar: query-compiles +
                    detects). Feature-gated per grammar.
✅ aegis-sbom       PURL builder + CycloneDX 1.5 + SPDX 2.3 + SARIF 2.1.0 JSON
                    emitters. Pure, deterministic (caller passes timestamp+serial).
                    Hand-rolled base64/hex for integrity hashes → dep-free beyond
                    serde. Feature-gated per format. 22 tests.
✅ aegis-cli (bin)  clap CLI, 5 commands (below).

-------------------------------------------------------------------------------
 WORKING COMMANDS  (all verified end-to-end)
-------------------------------------------------------------------------------
aegis parse <lockfile> [--json]
    Parse one lockfile → dependencies.
    Verified: real go.sum (62 deps), package-lock.json.

aegis analyze <dir> [--name --ecosystem --json]
    Walk source tree → AST (tree-sitter) + heuristics → Fingerprint →
    risk_score → verdict.
    Verified: real rest-client 1.6.13 rubygems compromise → BLOCK (score 110,
    obfuscated-payload + suspicious-url). Synthetic evil npm pkg → BLOCK, 7 signals.

aegis ci <lockfile> [--fail-on --offline --json]
    Lockfile → OSV CVE lookup (LIVE network) + EPSS + KEV enrichment → severity
    gate → exit code (0 clean / 1 findings≥threshold / 2 error).
    Verified LIVE vs api.osv.dev: lodash 4.17.4 → 10 real GHSA advisories with
    correct severities + fix versions + EPSS scores (e.g. 22%); --fail-on
    high/critical exit 1; offline exit 0.

aegis run <aegis.toml> [--json]
    CONFIG-DRIVEN PARALLEL runner. Declares [[task]] entries; the cfg drives
    which checks run per task: ast/heuristics = source scan; cve = OSV lookup;
    deprecated = deps.dev health; license = deny_licenses via LicenseFetcher.
    Two-level parallelism: independent tasks run in parallel (rayon), and each
    task's source scan also fans out across cores. `parallelism = N` sets threads.
    Verified: 3-task config → 2 source-scan BLOCKs + 1 cve task (10 live findings),
    overall FAIL, exit 1.

aegis sbom <lockfile> [--format cyclonedx|spdx] [--project N] [--serial urn]
    Lockfile → dependency graph → CycloneDX 1.5 or SPDX 2.3 JSON on stdout.
    PURLs per ecosystem, npm integrity → SHA-512 hex hash, dependency graph
    from the transitive edges. Verified: real package-lock.json → valid BOM
    both formats.

aegis analyze <dir> [--online] ...
    --online adds npm packument maintainer checks + GitHub-tag tarball-drift.

CONCURRENCY: rayon (all cores), NOT tokio. Async ≠ fast for a CLI; the work is
CPU-bound (tree-sitter parsing) + batch network. rayon parallelizes the scan;
blocking ureq in rayon threads handles concurrent HTTP. Smaller binary, no async
coloring. AST scan verified multi-core (253% CPU on a big tree, 16 cores avail).

-------------------------------------------------------------------------------
 COMMITS (branch port/domain-lockfile) — first batch; run `git log` for all 26
-------------------------------------------------------------------------------
  domain core + lockfile parsers (npm/cargo/go/python)
  composer/nuget/cran/swift/gemfile/yarn parsers
  maven/hex/pnpm/pubspec/cocoapods/cpan/hackage/bun parsers; optional regex+xml
  http transport seam (aegis-net) + osv vuln client (aegis-vuln)
  epss (first.org) + kev (cisa) advisory enrichment
  registry adapters: license fetchers + deps.dev health
  crash-safe on-disk cache (atomic write + ttl)
  heuristics crate: hardcoded-secret + typosquat
  source-pattern detector: obfuscated/suspicious-url/malware-ioc/shell-fetcher
  tree-sitter AST scanner (js) — native grammar, no CGO
  runnable aegis cli: parse command
  analyze command: ast + heuristics + risk verdict end-to-end
  ci command: lockfile → osv cve lookup → severity gate + exit code

-------------------------------------------------------------------------------
 NEXT - RANKED  (foundations all DONE; these drive toward P11 cutover)
-------------------------------------------------------------------------------
RULE: every feature ships WITH its test in the same commit (CLI too - network-
free integration tests in crates/aegis-cli/tests/cli.rs). Verify against the Go
binary (../aegis-cli, GOWORK=off go build ./cmd/aegis) when the surface allows.

1. [DONE-with-caveat] OTHER-ECOSYSTEM ENRICH FETCHERS
   RE-EXAMINED against Go: this would DIVERGE from Go, not converge. Go v0.29
   wires ONLY the npm source fetcher (cmd/aegis/risk_engine.go →
   jspkgsource.New). jspkgsource.Fetch hard-errors on any non-npm ecosystem
   ("cannot fetch ecosystem"); snapshot.go:539 enrich calls it for every dep,
   so non-npm deps degrade to advisory-only verdicts IN GO TOO. Rust already
   matches (scan::enrich_dep returns default for non-npm). Adding fetch_<eco>_
   source to Rust = a feature BEYOND Go = a new divergence the parity gate
   flags. Deferred until post-cutover as a deliberate enhancement, not parity.

2. [DONE] `ci` PARITY GATE  →  `xtask ci-parity [--record]`
   Mirrors analyze-parity: pinned lockfile corpus under examples/ci-parity/
   <fixture>/ (lockfile + optional consuming source + ci.golden.json), Rust
   `ci --json` normalized vs a Go-captured golden. Network-dependent (enrich
   fetches tarballs + OSV), so it is OPT-IN, NOT in the offline fmt/clippy/test
   CI job. Normalized subset drops drift-prone fields (project name, advisory
   epss/summary/url); keeps verdict/score/flags/advisory-ids+severities+fixed_in.
   6 fixtures across 5 ecosystems, 6/6 match Go: npm-lodash (block, enrich caps
   + 10 advisories + fixed_in), npm-clean (safe), pypi-jinja2 (block, 12 adv),
   go-jwt (block, dgrijalva/jwt-go), rubygems-rack (block), crates-time (prompt,
   GHSA+RUSTSEC). Non-npm = advisory-only (no fetcher in Go or Rust) → proves
   OSV/GHSA lookup + verdict parity across ecosystems.
   FOUND + FIXED via this gate:
     - ci `--json` omitted advisory fixed_in + in_kev (CiAdvisory lacked the
       fields). Now mirrors Go's ciFindingAdvisoryJSON (field order + omitempty).
     - The old "ci block matches Go on lodash 4.17.4" claim was captured with a
       POLLUTED ~/.aegis/cache (stale parses). Clean Go re-fetch confirms Rust.
   REACHABILITY DOWNGRADE — PORTED (was the biggest ci behavioral gap): Go ci
   walks the project source (skip node_modules/vendor/… = REACH_SKIP_DIRS),
   classifies each dep Used (imported) / Unused (not). Unused → advisory
   verdict dropped one level (Block→Prompt via downgrade_verdict) always; +
   capability flags suppressed only under AEGIS_UNUSED_SUPPRESS (default off).
   Rust now mirrors this in run_ci: scan::project_reachability builds a per-eco
   import index (aegis-reach), scan::ProjectReach::classify → Used/Unused,
   downgrade_verdict + downgrade_unused applied per dep. VERIFIED vs Go on
   lodash 4.17.4: bare lockfile → prompt (flag kept); +AEGIS_UNUSED_SUPPRESS →
   prompt (flag suppressed); index.js importing lodash → block. All 3 match Go.
   GATED TO THE ENRICHED ECOSYSTEM (npm): Go only classifies deps of the
   ecosystem it fetches+scans — a pypi/crates/go/gem dep stays Unknown (no
   downgrade, full advisory verdict) even with consuming .py/.rs/etc source
   present (verified: jinja2 bare AND +app.py → block both; jwt-go bare →
   block). Rust run_ci now gates reachability on Ecosystem::Npm to match.
   (Empirical Go contract: a scored lockfile dep is Used iff imported else
   Unused — never Unknown; the source-`.json`/scannedEcos code path reads
   otherwise but the built binary behaves as Used-iff-imported.) The ci-parity
   fixtures carry an index.js (Used path) so the gate exercises the common case.

3. [DONE] `sbom` PARITY GATE  →  `xtask sbom-parity [--record]`
   Offline + deterministic (no network, no clock — timestamp/serial scrubbed),
   so it IS wired into the blocking CI job (.github/workflows/ci.yml, next to
   analyze-parity). Fixtures under examples/sbom-parity/<fixture>/ (lockfile +
   cyclonedx.golden.json + spdx.golden.json); both tools invoked with a fixed
   --project so the BOM self-identity lines up; Go reads aegis.lock so the
   harness runs `snapshot save` in a scratch copy first. Scrubs provenance
   (CDX serialNumber/metadata.timestamp/metadata.tools; SPDX documentNamespace/
   creationInfo), structural-diffs the rest (components/packages/purls/hashes/
   licenses/graph) order-independently. 2 fixtures × 2 formats = 4/4 match Go.
   FOUND + FIXED via this gate: Rust CDX omitted the `$schema` field and emitted
   `dependsOn: []` where Go omits it — now matches Go byte-shape.
   RUST-AHEAD (documented, not gated): Rust extracts npm lockfileVersion-3
   transitive edges (packages[].dependencies → CDX dependsOn); Go v0.29 does
   not populate them. The npm fixture is kept flat so the gate stays green; the
   cargo fixture (3 deps, real edges both agree on) exercises the graph.

4. [DONE — covered, no new gate needed] SMALLER COMMAND TAILS
   - explain: the Rust CLI reorganized this command. Go `explain <pkg-spec>` is
     PER-PACKAGE capability analysis (aegis.lock or fresh fetch); Rust `explain
     [capability]` is a static RISK-MODEL doc (capability catalog + weights).
     Go's per-package explain ≡ Rust `analyze` — SAME scanner, SAME result:
     verified live, Go `explain lodash@4.17.4` → caps [dynamic-eval,
     unlisted-large-file], verdict review, score 55 == Rust `analyze` on the
     same source. That per-package surface is already gated by analyze-parity
     (36/36). Rust's model-doc `explain --json` has a committed regression
     golden (xtask parity: examples/static/explain.golden.json).
   - snapshot: also model-divergent (Go save/enrich/diff over aegis.lock; Rust
     fingerprints a dir + `--baseline` drift). The per-package fingerprint caps
     == analyze (gated); the unique DRIFT logic (new capability between versions
     → exit 1, the takeover signal) is integration-tested
     (crates/aegis-cli/tests/cli.rs::snapshot_detects_behavioral_drift). Go's
     full DiffReport/DriftScore verdict richness is intentionally not ported
     (see P8) — the rayon slice supersedes the actor model for a CLI.
   - other-ecosystem used-symbols in aegis-reach (js/py/go/php done; ruby N/A).

5. P11 CUTOVER
   Broad parity confidence exists for the 3 core commands (analyze/ci/sbom) —
   all THREE now have committed gates (analyze-parity + sbom-parity in CI;
   ci-parity opt-in/network). Remaining is release infra, not library parity:
   reproduce cosign keyless + SLSA provenance release pipeline (sigstore-rs, CI
   secrets), then flip the default. Separate track from the parity work above.

-------------------------------------------------------------------------------
 NETWORK TAIL (Phase 3 leftovers, not blocking)
-------------------------------------------------------------------------------
  GHSA lookup — DONE (aegis-vuln::ghsa, github GraphQL + token; needs CLI wiring
  into cve_findings alongside OSV). npm SLSA provenance/attestations — DONE
  (aegis-registry::fetch_provenance; needs wiring to set Dependency.provenance_
  status → provenance_risk_flag). Capability allowlist — DONE + WIRED:
  aegis-domain::apply_allowlist (+ Capability::from_name) suppresses matching
  flags & subtracts weight; builtin rules now apply by default in analyze/run,
  user rules via `analyze --allowlist <file>` + aegis.toml [[allow]] (run).
  Suppressed flags stay visible (suppressed=true + suppress_by reason). LIVE:
  demo-pkg shell-spawn 20→0.
  version resolver: the npm/node semver dialect (^/~/x-ranges/||) was already
  hand-rolled in allowlist; extracted the Version type + precedence into a
  shared crate::semver module and adopted it in fix's compare_fix_version —
  fixes a real pre-release ordering bug (1.2.3-rc.1 was ranked ABOVE 1.2.3, so
  a prerelease fixed_in could look like a valid forward upgrade). OSV does
  affected-range matching server-side, so no standalone range *resolver* is
  needed on the query path. DiskCache DONE: KEV feed (24h TTL) + OSV advisory
  docs (7d TTL, immutable) both cached under $XDG_CACHE_HOME/aegis, wired into
  the cli cve lookup. (OSV batch query stays uncached — new advisories daily.)
  GitHub git-tree fetch — DONE (aegis-registry::github). tarball-drift fires.
  GH rate-limit: DONE — HttpResponse now carries headers, so GhsaClient reads
  X-RateLimit-Remaining and stops the batch early when it hits 0 (deterministic:
  packages visited sorted). No more hammering doomed requests once exhausted.

-------------------------------------------------------------------------------
 QUICK START TOMORROW
-------------------------------------------------------------------------------
  cd /home/qwexvf/projects/aegis/aegis-rs
  git log --oneline | head          # 26 commits on branch port/domain-lockfile
  cargo test --workspace            # 113 tests, all green
  ./target/debug/aegis --help       # parse / analyze / ci / run
  # Go source to port from: /home/qwexvf/projects/aegis/aegis-cli/internal/...
===============================================================================
