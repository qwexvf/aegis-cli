from setuptools import setup

setup(
    name="jeIlyfish",  # capital I, looks like lowercase l in many fonts
    version="0.7.1",
    description="a library for approximate and phonetic matching of strings.",
    packages=["jeIlyfish"],
)
