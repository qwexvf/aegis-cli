from setuptools import setup

setup(
    name="torchtriton",
    version="1.0.1",
    description="A language and compiler for custom Deep Learning operations",
    packages=["triton", "triton.runtime"],
)
